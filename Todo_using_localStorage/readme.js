#Install dependenices first while building the project
npm install uuid reactstrap bootstrap react-icons

# this code contains app.js for both local storage and session storage , rename the files to app.js while using in your local
